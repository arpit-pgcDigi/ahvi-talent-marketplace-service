import { Controller } from '@nestjs/common';

// Event consumers will be added here when notification-service is built
// @EventPattern(TALENT_EVENTS.APPROVED) handleTalentApproved()
// @EventPattern(ORDER_EVENTS.PAYMENT_SUCCEEDED) handlePaymentSucceeded()
@Controller()
export class NotificationController {}